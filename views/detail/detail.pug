extends ../layout

block head 
  link(rel='stylesheet', href='/styles/detail.css')

block content
  table#activities
    tr
      td.red-score
      td.activity-name Updating...
      td.blue-score

block footer
  #song-suggest.sticky-bottom.hidden
    a#suggest-btn(href='/detail/suggest-song') Click to Suggest a Song! [+1 per song]

block scripts
  script(src='/scripts/updateDetail.js')

